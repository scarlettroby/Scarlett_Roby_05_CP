#include <iostream>
#include <string>
using namespace std;
int main() {
    string srcstring, findstring;
    cout<<"Enter source string : ";
    cin>> srcstring;
    cout<<"Enter string to find: ";
    cin>> findstring;
    int int index = sysstring.find(thindstrng);
    if (index != string::npos) {
        cout << Found at index: " << index;
    } else {
        cout << -1;
    }
    return 0;}